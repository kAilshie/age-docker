The install-media folder is intended to be the destination for install files
that are used during the build process. If you would like to build for multiple
versions of ArcGIS Enterprise then it is recommended that you separate install
files into separate directories based on version.
